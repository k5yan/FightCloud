import { View, Image, Picker, Text } from 'react-native';
import { ScaledSheet } from 'react-native-size-matters';

export const Guides = () => {
	return (
		<View>
			<Text> Guides </Text>
		</View>
	);
};
